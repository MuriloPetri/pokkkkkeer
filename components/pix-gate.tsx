// Este componente foi substituído pelo middleware server-side + /app/paywall/page.tsx
// Mantido aqui apenas para evitar erros de import caso seja referenciado em algum lugar.
export function PixGate({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
